# Config store validation service

This small Go service stores string values in memory and exposes `PUT` and `GET` endpoints under `/v1/records/{key}`.

Run the test suite with:

```text
go test ./...
```

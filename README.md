# intervalmerge

`intervalmerge` is a small Go module for normalizing and merging closed integer intervals.

Run the tests with `go test ./...` or the example command with `go run ./cmd/intervalmerge`.

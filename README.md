# ledgerbatch

`ledgerbatch` is a small Go package for applying account balance changes as an
all-or-nothing batch.

Run the tests with `go test ./...` or the example command with
`go run ./cmd/ledgerbatch`.

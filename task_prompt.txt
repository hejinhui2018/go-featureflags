pagestore 里已经有两条记录时，调用 List(5, 2) 会直接 panic。分页读取允许 offset 落在当前记录末尾之后，这种情况应该返回空页和 nil error，服务不能因为一次越界分页请求崩溃。

请修复这个行为并补充回归测试。正常分页、尾页截断、offset 恰好等于记录数、负 offset、非正 limit，以及返回结果不影响存储内容的现有行为都要保持不变。完成后运行相关测试、go test ./...、go vet ./... 和 go build ./...，如实报告结果。

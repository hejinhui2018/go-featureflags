# 并发幂等请求复现

## 可观察行为

同一个 `Idempotency-Key` 的两个 POST 请求同时到达时，业务处理会执行两次，两个请求分别拿到不同的执行编号和响应正文。顺序发送相同 key 时能够复用响应，不同 key 也能分别处理。

## 复现命令

```powershell
go test ./... -run '^TestConcurrent' -count=20
```

期望并发的相同 key 只执行一次业务处理，等待中的请求复用第一次请求的状态码、响应头和正文；不同 key 的并发请求仍能彼此独立地执行。

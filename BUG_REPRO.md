# 超时响应被迟到的处理结果改写

## 复现

在项目目录执行：

```text
go test ./... -run '^TestTimeoutDiscardsLateHandlerWrite$' -count=20
```

这个请求处理器会先等待外部信号，超过 20 毫秒后才继续完成。实际结果中，客户端虽然已经收到超时响应，之后仍可能看到处理器补写的响应头或响应正文，正文会同时包含超时提示和处理器结果。

## 期望

一旦返回超时响应，后续处理器结果不应再改变这次请求的任何响应头、状态码或正文。处理器在未超时时的正常响应，以及其他请求处理行为，都应保持不变。

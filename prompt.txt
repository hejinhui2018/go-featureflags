请修复这个 Go 服务的一个输入校验问题，并保留一条完整的诊断、修改和验证过程。

当客户端调用 `PUT /v1/records/{key}` 时，请求体必须包含字符串字段 `value`。目前请求体为 `{}` 时服务仍返回 200，并把记录写成空值；这种请求应返回 400 和 JSON 错误 `invalid value`，并且不能创建或覆盖记录。包含正常字符串 value 的请求、格式错误的 JSON、GET 已有记录和 GET 不存在记录的状态码与错误语义都要保持不变。

请在代码中修复行为，补充针对缺少 value 字段的回归测试，并运行相关测试以及 `go test ./...`、`go vet ./...`、`go build ./...`。只提交与这个问题相关的修改。
